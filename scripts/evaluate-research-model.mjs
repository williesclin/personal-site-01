// Fail-closed release gate. This does not train a model or invent ground-truth labels.
import {readFile,writeFile} from 'node:fs/promises';
const feed=JSON.parse(await readFile(new URL('../data/research-feed.json',import.meta.url),'utf8'));
const labelsPath=process.env.QP_REVIEWED_LABELS;
const labels=labelsPath?JSON.parse(await readFile(labelsPath,'utf8')):[];
const {validateReviews}=await import('./review-validation.mjs');
const review=validateReviews(labels,feed.documents);
const report={evaluatedAt:new Date().toISOString(),task:'filing_topic_and_sentiment',status:'blocked',releaseAllowed:false,observedDocuments:feed.documents.length,reviewedLabels:review.uniqueDocuments,reviewCoverage:review,macroF1:null,calibrationError:null,baselineComparison:null,reason:review.uniqueDocuments<100?'Need at least 100 independently reviewed, source-linked labels before temporal model evaluation.':'Labels are available; no trained candidate or frozen temporal benchmark has been executed.',requiredGates:{minimumReviewed:100,minimumTemporalHoldout:30,minimumClasses:3,minimumPerClassHoldout:5,groupByIssuerAndEvent:true,split:'time-ordered; no future observations; duplicate events stay together',baseline:'majority and SEC form/item rule',quality:'macro-F1 >= 0.75 and >= baseline + 0.05; report class precision/recall; ECE <= 0.10 for probabilistic outputs',humanReview:'At least 20% independently double reviewed; record disagreements',alerts:'source-linked; >=20 distinct events; >=90% independent relevance review; zero duplicates; 7-day shadow observation; no trading recommendations'},limitations:['SEC form classification is a deterministic metadata rule, not AI sentiment.','Official company announcements and social opinions have different source biases.','No social API, sentiment ground truth or paid model service connected.']};
await writeFile(new URL('../data/research-model-readiness.json',import.meta.url),JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));
